# /etc/systemd/zram-generator.conf
# NexusOS ZRAM Configuration — 8GB Physical RAM System
# Mathematical basis: zram-math.md

[zram0]
zram-size = ram
compression-algorithm = zstd
fs-type = swap
swap-priority = 100
